<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:tal="http://xml.zope.org/namespaces/tal"
      xmlns:metal="http://xml.zope.org/namespaces/metal"
      metal:use-macro="layout">


<tal:block metal:fill-slot="main">
    <!-- Main hero unit for a primary marketing message or call to action -->
    <div class="hero-unit">
        <h1>${archive.label}</h1>
        <i>Uploaded by ${archive.submitter_name} on ${archive.time.strftime("%c")}</i>
        <p>You may always view this report here on this page <a href="${request.route_url('check', archive_id=archive.id)}">${request.route_url('check', archive_id=archive.id)}</a></p>
        <p>This archive is stored in <pre>${archive.path}</pre></p>
    </div>
    <div>
        <h2>Status: ${archive.status}</h2>
        <table class="zebra-striped">
            <thead>
                <tr>
                    <th>Diagnostic</th><th>Status</th><th>Details</th>
                </tr>
            </thead>
            <tbody>
                <tr tal:repeat="test archive.diagnostics">
                    <td>${test.name}</td>
                    <td>
                        <span class="label ${test.status}">${test.status}</span>
                    </td>
                    <td>${test.details}</td>
                </tr>
            </tbody>
        </table>
    </div>
</tal:block>

</html>
